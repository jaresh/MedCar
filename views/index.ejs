<!doctype html>
<html>
<% include partials/header %>   
<body>

	<% include partials/menu %>   

	<div id="content">
		<% if (user.type == 'user') { %>
			<p>Strona pacjenta</p>
		<% } %>

		<% if (user.type == 'admin') { %>
			<p>Strona administratora</p>
		<% } %>

		<% if (user.type == 'doc') { %>
			<p>Strona doktora</p>
		<% } %>
	</div>

	<% include partials/footer %>   
</body>
</html>